<?xml version="1.0" encoding="UTF-8"?>
<!-- Logging disabled — delegates entirely to Spring Boot defaults (console only). -->
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>
</configuration>
